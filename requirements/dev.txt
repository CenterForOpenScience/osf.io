-r ../requirements.txt

# Requirements that are used in the development environment only

# Testing
pytest==7.4.4
pytest-socket==0.7.0
pytest-xdist==3.5.0
pytest-django==4.8.0
python-coveralls==2.9.3 # do we need it?
pytest-testmon==2.1.0
pytest-asyncio==0.23.5  # The last version that supports Python 3.6 and pytest 5.0
asynctest==0.13.0
factory-boy==3.3.0
webtest-plus==1.0.0
Faker==23.2.1
schema==0.7.4
responses==0.25.0

# Syntax checking
flake8==7.0.0
flake8-mutable==1.2.0
pre-commit==1.10.5

# Django Debug Toolbar for local development
django-debug-toolbar==3.2.4

# Ipdb
ipdb==0.10.1

# PyDevD (Remote Debugging)
pydevd==0.0.6

# n+1 query detection
nplusone==0.8.2

# Profiling
django-silk==4.2.0  # pyup: <3.0 # Remove this when we're using MIDDLEWARE
