from framework.routing import Rule, json_renderer
from website.routes import OsfWebRenderer

settings_routes = {
    'rules': [
    ],
    'prefix': '/api/v1/'
}

api_routes = {
    'rules': [
    ],
    'prefix': '/api/v1/'
}

nonapi_routes = {
    'rules': [
    ]
}
