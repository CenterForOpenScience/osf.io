Hello ${user.fullname},

We just wanted to let you know that ${initiated_by} has initiated the following pending registration: ${registration_link}${context.get('custom_message', '')}.

If approved, a registration will be created for the project, viewable here: ${registration_link}, and it will remain
public until it is withdrawn.

Sincerely yours,

The OSF Robots
