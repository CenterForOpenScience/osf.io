import { canRemoveContributor } from 'website-ember/helpers/can-remove-contributor';
import { module, test } from 'qunit';

module('Unit | Helper | can remove contributor');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = canRemoveContributor([42]);
  assert.ok(result);
});
