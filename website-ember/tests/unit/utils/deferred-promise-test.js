import deferredPromise from 'website-ember/utils/deferred-promise';
import { module, test } from 'qunit';

module('Unit | Utility | deferred promise');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = deferredPromise();
  assert.ok(result);
});
